FROM node:20-alpine

WORKDIR /app

# Copy package files first (for better caching)
COPY frontend/package*.json ./

# Install dependencies during build (platform-specific)
RUN npm install

# Copy the rest of the frontend code
COPY frontend/ ./

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]